name: Build IPA

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    env:
      SCHEME: PS5PayloadSender
      PROJECT: PS5PayloadSender.xcodeproj
      ARCHIVE_PATH: ${{ github.workspace }}/build/PS5PayloadSender.xcarchive
      EXPORT_PATH: ${{ github.workspace }}/build/ipa

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Build Archive
        run: |
          xcodebuild archive \
            -project "$PROJECT" \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$ARCHIVE_PATH" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            PRODUCT_BUNDLE_IDENTIFIER=com.local.ps5payloadsender \
            IPHONEOS_DEPLOYMENT_TARGET=16.0

      - name: Show archive contents (debug)
        run: find "$ARCHIVE_PATH" -type f | head -40

      - name: Package IPA (unsigned)
        run: |
          APP=$(find "$ARCHIVE_PATH/Products/Applications" -name "*.app" -maxdepth 1 | head -1)
          echo "Found app: $APP"
          mkdir -p Payload
          cp -r "$APP" Payload/
          mkdir -p "$EXPORT_PATH"
          zip -r "$EXPORT_PATH/PS5PayloadSender.ipa" Payload/
          rm -rf Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: PS5PayloadSender-IPA
          path: ${{ env.EXPORT_PATH }}/PS5PayloadSender.ipa
          retention-days: 7
